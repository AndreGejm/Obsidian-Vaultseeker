{
  "name": "@vaultseer/obsidian-plugin",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && node esbuild.config.mjs production",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@vaultseer/core": "workspace:*"
  },
  "devDependencies": {
    "builtin-modules": "^3.3.0",
    "esbuild": "0.25.0",
    "obsidian": "^1.8.7",
    "vitest": "^3.0.0"
  }
}
